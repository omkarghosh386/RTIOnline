import React from 'react';
import { Container } from 'react-bootstrap';

const ViewStatus = () => {
  return (
    <Container className="mt-5 text-center">
      <h2 className="text-primary mb-4">View Status</h2>
      <p>This is the demo page for View Status functionality.</p>
    </Container>
  );
};

export default ViewStatus;
